{
  "name": "pay2play",
  "version": "0.1.0",
  "private": true,
  "description": "Agnostic nanopayment infrastructure for Circle Arc — meter anything, settle in USDC",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "test": "pnpm -r --filter=./packages/* test",
    "lint": "pnpm -r lint",
    "chain-id-check": "tsx scripts/chain-id-check.ts",
    "generate-wallets": "tsx scripts/generate-wallets.ts",
    "fund": "tsx scripts/fund.ts",
    "bench": "tsx scripts/bench.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=22"
  }
}
